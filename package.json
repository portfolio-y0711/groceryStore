{
  "name": "postype",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@commitlint/config-conventional": "^13.1.0",
    "husky": "^7.0.1"
  },
  "scripts": {
    "push:front": "git subtree --prefix backend push @frontend main",
    "push:back": "git subtree push --prefix backend @backend main",
    "remote:start": "bash scripts/deploy-remote.sh"
  }
}
